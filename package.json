{
  "name": "eah",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "start": "NODE_ENV=production bun src/server.ts",
    "migrate": "bun scripts/migrate.ts",
    "seed-admin": "bun scripts/seed-admin.ts",
    "drop-legacy": "bun scripts/drop-legacy.ts"
  },
  "dependencies": {
    
    "mariadb": "^3.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
